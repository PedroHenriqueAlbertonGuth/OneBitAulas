let crew: [string, string, string] = ["Pedro", "Henrique", "Alberton"];

let point: [number, number];

point = [2, 5];

let [x, y] = point;

enum Planets {
  MERURIO = 1,
  VENUS = "Venus",
  TERRA = "Terra",
  MARTE = "Marte",
}

Planets.MERURIO;

enum Roles {
  ADMIN,
  USER,
}

Roles.ADMIN